import express, { Request, Response } from 'express';

const app = express();

/**
 * @Swagger
 *  "user": 
 *      id: Integer
 *      name: String
 */

interface alsd {

}

app.listen(3000, function () {
    console.log('Ready')
  })
